rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ログインしていればOK（本番時はもっと厳しくします）
      allow read, write: if request.auth != null;
    }
  }
}